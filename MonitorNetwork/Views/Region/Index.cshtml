@model IEnumerable<MonitorNetwork.Models.RegionStoreRelayModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.region.regionColor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.region.regionColor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.region.regionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.region.regionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.region.regionID })
        </td>
    </tr>
}

</table>
